# Documentation

This directory records the public architecture, provenance, licensing
boundaries, milestone scope, and QA model for the integration repository.
Private manuals and private-derived facts are excluded.

M01 build design and evidence are documented in `build/`, with the canonical
toolchain and upstream build contracts recorded separately from generated
ignored results.
