# Repository Layout

The parent repository owns integration metadata, profiles, overlays, manifests,
build orchestration placeholders, and QA definitions. `components/` contains
gitlinks to the three foundational repositories. `images/output/` and other
generated locations are reserved but empty in M00.

VAEG and non-public PC-88VA material are sibling checkouts, not repositories or
submodules here.
