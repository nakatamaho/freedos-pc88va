# M03 M04 Blocker Ledger

Every row is intentionally `UNKNOWN`. The candidate source IDs identify where
review may look; they do not mean the source has already established the VA
fact. No blocker is resolved by M03.

| question_id | subsystem | exact question | why it blocks implementation | required source category | candidate source IDs | current state | accepted evidence needed | downstream decision | target |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M04-BOOT-IPL-LOAD | boot | At what physical/segment address is the VA IPL loaded? | Loader placement and relocation cannot be chosen safely. | PC-88VA primary boot documentation | `PRV-VA-MANUAL` | UNKNOWN | Printed/manual or authoritative firmware statement with page reference and reviewed provenance. | IPL origin and relocation design | M04 |
| M04-BOOT-ENTRY-STATE | boot | What are CS, DS, ES, SS, SP, interrupt state, and boot-drive register values at IPL entry? | The first instruction and stack cannot be specified from NEC98 code. | PC-88VA primary boot documentation or ROM service specification | `PRV-VA-MANUAL`, `PRV-NEC98-BIOS` | UNKNOWN | VA-specific entry-state evidence; PC-98 material is insufficient. | IPL prologue and stack setup | M04 |
| M04-BOOT-SECTOR-FORMAT | boot | What sector size, boot length, signature, reserved bytes, and acceptance rules does VA firmware require? | A boot sector could be rejected or overwrite firmware-owned bytes. | PC-88VA firmware/boot documentation | `PRV-VA-MANUAL` | UNKNOWN | VA-specific format table or observed approved firmware behavior with reproducible setup. | Boot-sector layout | M04 |
| M04-FLOPPY-GEOMETRY | disk | Which floppy geometries, tracks, heads, sectors, and physical sector sizes are supported? | FAT/BPB and loader read loops depend on geometry. | PC-88VA drive/firmware documentation | `PRV-VA-FDD` | UNKNOWN | VA-specific geometry table and media qualification evidence. | Media matrix and BPB values | M04 |
| M04-BPB-MEDIA | disk | How are BPB fields, media identifiers, hidden sectors, and physical/logical sectors interpreted? | A syntactically valid BPB may still map to the wrong media. | PC-88VA boot/media documentation | `PRV-VA-MANUAL`, `PRV-VA-FDD` | UNKNOWN | VA-specific BPB interpretation and accepted media identifiers. | FAT12/media contract | M04 |
| M04-DISK-SERVICE | firmware | Does the loader use a VA BIOS service, direct FDC access, or a documented hybrid? | Choosing INT calls or ports without evidence can hang or corrupt media. | PC-88VA firmware and FDC documentation | `PRV-VA-MANUAL`, `PRV-VA-FDD` | UNKNOWN | VA service ABI or controller contract with register semantics. | Block-I/O adapter boundary | M04 |
| M04-FDC-DMA-IRQ | dma | What FDC command, DMA channel/address, interrupt vector, reset, and media-change contract is required? | Sector reads and change detection need exact ownership and timing. | PC-88VA chipset/FDC/DMA documentation | `PRV-VA-FDD`, `PRV-NEC98-DMA` | UNKNOWN | VA-specific register/IRQ/DMA tables and a bounded diagnostic test plan. | Loader and kernel disk driver interface | M04 |
| M04-KERNEL-LOAD | memory | Where does the boot path load the kernel, how many bytes/sectors transfer, and what registers are passed at entry? | The kernel cannot initialize or relocate safely without a contract. | PC-88VA boot/kernel integration documentation | `PRV-VA-MANUAL` | UNKNOWN | VA-specific load/transfer/entry specification, including failure behavior. | Kernel startup adapter | M04 |
| M04-EARLY-CONSOLE | console_output | What is the minimum diagnostic output route available before the kernel console is initialized? | Failure reporting and first-boot diagnosis otherwise have no defined channel. | PC-88VA firmware/console documentation | `PRV-VA-TEXT`, `PRV-VA-MANUAL` | UNKNOWN | VA-specific firmware or memory-mapped console contract and a safe early test. | IPL and early kernel diagnostics | M04 |
| M04-BOOT-INTERRUPTS | interrupts | Which vectors and interrupt-enable state are reserved or usable during IPL and kernel handoff? | Installing handlers can collide with firmware or the loader. | PC-88VA firmware documentation | `PRV-VA-MANUAL`, `PRV-NEC98-BIOS` | UNKNOWN | VA vector map and entry/handoff rules. | Interrupt initialization | M09 |
| M04-BOOT-TIMER | timer_clock | What timer source, tick rate, and firmware ownership apply before DOS initialization? | Clock services and timeout loops require a stable source. | PC-88VA chipset/firmware documentation | `PRV-VA-MANUAL` | UNKNOWN | VA timer register/vector contract and timing evidence. | Timer adapter | M09 |
| M04-KEYBOARD-ENCODING | console_input | What keyboard protocol, scan-code translation, and Japanese/DBCS encoding does VA firmware expose? | Input and filename behavior cannot be selected from NEC98 tables. | PC-88VA keyboard/text documentation | `PRV-VA-KEYB`, `PRV-VA-TEXT` | UNKNOWN | VA-specific keyboard ABI plus encoding table and review provenance. | Console input and M06 DBCS matrix | M06 |

The private candidates are metadata-only registrations. Their presence does
not promote any row to `DOCUMENT_FACT`; page-level review and source-specific
acceptance are still required. PC-98 and NEC98 sources may explain the
baseline, but cannot resolve a PC-88VA row.
