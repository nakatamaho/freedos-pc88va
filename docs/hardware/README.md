# Hardware

Real-hardware validation is optional and non-blocking for this repository. Only
an actual PC-88VA result may receive the `HARDWARE PASS` label. M00 performs no
hardware testing and makes no boot claim.
