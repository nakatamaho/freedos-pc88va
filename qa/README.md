# QA

QA is layered so host checks, VAEG checks, and manual hardware evidence are not
confused. M01 runs host build, reproducibility, and scaffold validation only.
VAEG and hardware evidence are `NOT RUN`.
