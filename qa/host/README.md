# Host QA

Host QA covers static tree, gitlink, lockfile, source-export, toolchain,
artifact, and reproducibility checks that do not require private artifacts or
a target machine. M01 results are retained under ignored `qa/results/m01/`.
