# Manifests

Manifests describe reproducible component pins and future package choices.
FreeDOS userland consists of many independent packages, so M00 does not select
package repositories or turn unselected packages into submodules. Package
licenses must be checked before any future distribution.
