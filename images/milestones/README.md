# Milestone distributions

Store one designated public, source-built D88 per milestone in `mNN/`,
compressed with xz. Each directory includes its source/build identities,
uncompressed and compressed SHA-256 digests, extraction instructions and
license references. ROMs, private test media, runtime results and intermediate
build products do not belong here.
