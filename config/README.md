# Configuration

Configuration is intentionally a placeholder in M00. Public profiles and
future image stages may refer to local paths through environment variables,
but secrets and machine-specific paths must remain local.
